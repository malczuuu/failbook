version: 3
tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list

  build:
    desc: Build application
    cmds:
      - rm -rf dist/ && mkdir dist/
      - go build -o ./dist/ ./cmd/failbook
